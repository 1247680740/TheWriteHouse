1;
var num;
10 - parseInt((Math.random() * 20));
2;
getRanStr();
string;
{
    var num_1;
    while (num_1 != 79) {
        var ranNum = Math.ceil(Math.random() * 25); //生成一个0到25的数字
        num_1 == ranNum;
        //大写字母'A'的ASCII是65,A~Z的ASCII码就是65 + 0~25;然后调用String.fromCharCode()传入ASCII值返回相应的字符
        return String.fromCharCode(65 + ranNum);
    }
}
3;
getRanNum: (function () {
    return 1 + Math.floor(Math.random() * Math.random() * 10);
});
4;
5;
6;
getMNum(let, n, number, let, m, number);
{
    for (var i = 0; i < n && m; i++) {
        if (Math.random() % (n - i) < m) {
            cout << i << "\t";
            m--;
        }
    }
    return;
}
//# sourceMappingURL=test.js.map